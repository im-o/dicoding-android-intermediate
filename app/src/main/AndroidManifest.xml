<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.rivaldy.id.dicoding">

    <uses-feature android:name="android.hardware.camera" />

    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:name=".App"
        android:allowBackup="true"
        android:icon="@drawable/dicoding_icon"
        android:label="@string/app_name"
        android:roundIcon="@drawable/dicoding_icon"
        android:supportsRtl="true"
        android:theme="@style/Theme.DicodingIntermediate">
        <activity
            android:name=".util.CameraActivity"
            android:exported="false" />
        <activity
            android:name=".ui.home.add_story.AddStoryActivity"
            android:exported="false" />
        <activity
            android:name=".ui.home.detail_story.DetailStoryActivity"
            android:exported="false" />
        <activity
            android:name=".ui.home.HomeActivity"
            android:exported="false" />
        <activity
            android:name=".ui.auth.register.RegisterActivity"
            android:exported="false" />
        <activity
            android:name=".ui.auth.login.LoginActivity"
            android:exported="false" />
        <activity
            android:name=".ui.MainActivity"
            android:exported="true"
            android:theme="@style/Theme.App.Starting">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <receiver
            android:name="com.rivaldy.id.dicoding.ui.stack_widget.ImageStoryWidgetProvider"
            android:exported="false">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>

            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/image_story_widget_info" />
        </receiver>
        <service
            android:name="com.rivaldy.id.dicoding.ui.stack_widget.StackWidgetService"
            android:permission="android.permission.BIND_REMOTEVIEWS" />
    </application>

</manifest>